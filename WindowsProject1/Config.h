#pragma once

#include <windows.h>

void init(
    int* WIDTH,
    int* HEIGHT,
    int* N,
    COLORREF* gridColor,
    COLORREF* backgroundColor);

void save(
    int* WIDTH,
    int* HEIGHT,
    int* N,
    COLORREF* gridColor,
    COLORREF* backgroundColor);

